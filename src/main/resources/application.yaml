quarkus:
  datasource:
    db-kind: postgresql
    username: janus
    password: janus

    jdbc:
      url: jdbc:postgresql://localhost:5432/janus?currentSchema=public
      max-size: 20
      min-size: 5
      telemetry: true

  flyway:
    migrate-at-start: true
    locations: db/migration